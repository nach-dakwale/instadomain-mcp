name: instadomain
description: >
  AI agents check, suggest, and buy domains. Pay with Stripe (card) or x402 (USDC on Base).
  L402 and MPP coming soon. Includes Cloudflare DNS setup, WHOIS privacy, domain transfers,
  and renewals.
startCommand:
  type: http
configSchema:
  type: object
  required: []
  properties: {}
