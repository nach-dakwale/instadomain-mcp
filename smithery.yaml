name: instadomain
description: >
  AI agents buy domains autonomously via x402 (USDC on Base), no human in the loop.
  Also supports Stripe checkout for manual approval. Includes Cloudflare DNS setup,
  WHOIS privacy, domain transfers, and renewals.
startCommand:
  type: http
configSchema:
  type: object
  required: []
  properties: {}
